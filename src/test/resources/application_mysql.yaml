spring:
  datasource:
    url: jdbc:mysql://localhost:3306/gpms?tlsVersions=TLSv1.2
    username: gpms
    password: rot2haar
    driver-class-name: com.mysql.cj.jdbc.Driver
    initialization-mode: always
  sql:
    init:
      mode: always
  jpa:
    defer-datasource-initialization: true
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: create
      show_sql: true
